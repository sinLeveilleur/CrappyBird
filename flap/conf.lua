function love.conf(t)
    -- Window settings
    t.window.title = "fluppyBard:O"  -- Window title
    t.window.width = 500               -- Initial width (pixels)
    t.window.height = 500               -- Initial height (pixels)
    t.window.fullscreen = false         -- Start in fullscreen? (true/false)
    --t.window.resizable = true           -- Allow window resizing?
    --t.window.vsync = 1                  -- Enable vertical sync (0 to disable)
    --t.window.msaa = 4                   -- Anti-aliasing (0, 2, 4, 8, 16)

    -- Optional: Set a window icon (requires .png or .ico)
    --t.window.icon = "assets/icon.png"   -- Path to your icon file
end